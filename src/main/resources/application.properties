spring.application.name=yesterdaysnews

# Common settings (optional)
# spring.datasource.driver-class-name=org.h2.Driver
# spring.jpa.hibernate.ddl-auto=update

# Uncomment this for H2 (dev)
# spring.datasource.url=jdbc:h2:mem:yesterdaysnews
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2

# spring.datasource.username=sa
# spring.datasource.password=

# Database configuration
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME}
spring.jpa.hibernate.ddl-auto=update

# Habilitar colores en los logs
spring.output.ansi.enabled=always

# Configurar colores para diferentes niveles de log
logging.level.root=INFO
logging.pattern.console=%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(%5p) %clr([%t]){cyan} %clr(%c){blue} : %m%n%wEx